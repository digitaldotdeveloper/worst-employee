# ---------------------------------------------------------------
# PRIVATE DESIGN DOCS — never publish these.
# GitHub Pages needs a PUBLIC repo on a free account, so anything
# committed here is world-readable. The financial plan in particular
# contains personal budget / banking notes.
# ---------------------------------------------------------------
Game Script.txt
Financial Plan.txt
_CONTINUE-HERE.md

# scratch
*.bak
*.log
node_modules/
.DS_Store
Thumbs.db

# Python bytecode from tools/*.py — a build artefact that kept showing as
# modified in every status and drifting into commits it did not belong in.
tools/__pycache__/
__pycache__/
*.pyc

# ---------------------------------------------------------------
# GENERATOR OUTPUT — never commit this again.
# The raw green-screen renders were 122MB in a PUBLIC repo, ~25x the
# size of the game itself, and the game does not load one byte of them
# at runtime: `tools/cutout.py` turns them into `assets/`, and `assets/`
# is what ships. They live in
#   Desktop/WORK-RELATED-source-archive/renders/
# Point the cutout tools at that folder when you need to re-cut a set.
# ---------------------------------------------------------------
tools/renders/
tools/renders-*/
tools/faces/
tools/rig/
tools/world/
tools/weapons/
tools/concepts/
