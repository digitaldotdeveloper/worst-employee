{
  "summary": "Map integration points in the Worst Employee codebase, then run a judge panel on weapon + coin-unlock architecture",
  "agentCount": 11,
  "logs": [
    "mapped 5/5 subsystems",
    "[design:design-depth] failed: API Error: Connection lost mid-response. The response above may be incomplete.",
    "2 designs produced",
    "ONE SLOT — weapons as the carried object, specials as the heavy: 18/40 | PAYROLL — Two Payslips, One Bank: 16/40"
  ],
  "result": {
    "plan": "# WORST EMPLOYEE — WEAPONS + COIN-UNLOCK: DEFINITIVE IMPLEMENTATION PLAN\n\nVerified against the live files (`js/player.js` 236 lines, `js/main.js` 746, `js/config.js` 120, `js/chaos.js` 83, `js/art.js` 511, `js/office.js` 225, `index.html` 159, `css/game.css` 197). Every line reference below is real.\n\n---\n\n## 0. THE FIVE DECISIONS THAT MAKE THIS BUILDABLE\n\nThese are settled. Everything downstream follows from them.\n\n**D1 — An equipped weapon lives in a NEW slot `p.equipped`, never in `p.carrying`.**\n`_startAttack` becomes `const wep = this.carrying || this.equipped;` (grabbed prop wins). This single choice kills the two worst regressions found in review: `art.js:260` (`if (p.carrying) return 'swing'`) and `art.js:274` (`if (p.carrying) return 'carry'`) stay untouched and **all 28 drawn frames keep playing** while equipped, and `_grabOrThrow` (player.js:186) stays untouched so **the GRAB button still grabs**. `art.js` and `rig.js` are not edited at all.\n\n**D2 — The equipped weapon Body is NOT in `S.world.bodies`.**\nIt is a standalone `Body` hanging off the player. It therefore never collides, never gets a contact shadow, is never drawn by the props loop at main.js:466-469, is never seen by `_swing`'s target loop, and — critically — **is never passed to `S.damageBody`**, so it cannot ever set `broken`, increment `S.destroyed`, add to `S.damage`, drop productivity, or raise anger. That closes the \"value:0 only covers one of five side effects\" flaw completely rather than patching one symptom.\n\n**D3 — Equipped weapons cannot be thrown.** The grab button keeps exactly one meaning: pick up / hurl *world props*. There is no grip-pop-on-throw, no weapon breaking from a chaos impact, no lost-weapon recovery scan, no `recall` flag. Chair-throwing is still in the game — `buildOffice` spawns seven grabbable chairs (office.js:179).\n\n**D4 — Depth-1 ignites pay zero coins.** `chaos.js:35` becomes `CHAOS.coinBase * (depth - 1) * chaosMul`. The per-hit `ignite` at player.js:170 becomes a chain *seed* only. This closes the broken-prop farm (re-hitting a dead prop pays 0 forever), makes chains the sole chaos earner, and means a weapon can never be a money printer by hitting fast.\n\n**D5 — Quiet play gets a real wage.** `S.salary` ticks every second with a clean-streak multiplier. Both playstyles land in the same 10k–13k band per shift. The shop is reachable without destroying one object.\n\n---\n\n## 1. THE WEAPON TABLE — `js/weapons.js` (NEW FILE)\n\nPaste-ready. Six weapons; each has one verb no other weapon has, not a bigger number.\n\n```js\n// js/weapons.js — WEAPONS, their stat blocks, their specials, their skills.\n// Rule that must never be broken: nothing in here ever clones, spreads or\n// mutates a row of ATTACK. Weapons contribute SCALARS applied alongside `d`,\n// so the three `d === ATTACK.heavy` reference checks in player.js (:152 lunge,\n// :173 sparks, :175 hit sound) keep working.\n\nimport { ATTACK, PLAYER } from './config.js';\nimport { FX } from './fx.js';\nimport { SFX } from './audio.js';\nimport { Body, rectsOverlap } from './engine.js';\n\n// ── counters ───────────────────────────────────────────────────────────────\n// bump() is the ONLY way a counter moves. It checks unlocks on the spot, so\n// the toast fires mid-shift and the save is written immediately.\nexport function bump(s, key, n = 1) {\n  const c = s.career.counters;\n  c[key] = (c[key] || 0) + n;\n  checkUnlocks(s);\n}\nexport function setMax(s, key, v) {\n  const c = s.career.counters;\n  if (v > (c[key] || 0)) { c[key] = v; checkUnlocks(s); }\n}\nexport function has(s, id) { return s.career.skills.includes(id); }\n\nexport function checkUnlocks(s) {\n  const c = s.career.counters;\n  for (const id of s.career.owned) {\n    const w = WEAPONS[id]; if (!w) continue;\n    for (const sk of w.skills) {\n      if (s.career.skills.includes(sk.id)) continue;\n      if (!sk.req(c)) continue;\n      s.career.skills.push(sk.id);\n      s.careerDirty = true;\n      s.toast(sk.name + ' — UNLOCKED', 'boss');\n      SFX.promote();\n    }\n  }\n}\n\n// ── stat blocks ────────────────────────────────────────────────────────────\n// FIST_STATS reproduces today's unarmed numbers exactly: reach = d.reach + 0,\n// hh = d.hh + 0, mul = 1, and the wep?1.4:1 / 1.35 / 1.5 switches become 1.\nexport const FIST_STATS = {\n  reach: 0, hh: 0, dmgMul: 1, kbMul: 1, spd: 1,\n  shakeMul: 1, stopMul: 1, sfxMul: 1, sparkMul: 1,\n  seedDepth: 1, grip: 0.5, wear: 0,\n  special: null, onSwing: null, onHit: null, box: null,\n};\n\n// propStats() IS player.js:143-145 lifted verbatim. A grabbed monitor behaves\n// bit-for-bit as it does today.\nexport const propStats = (wep) => {\n  const m = 1.6 + Math.min(1.2, wep.mass * 0.35);\n  return { reach: wep.w * 0.8 + 10, hh: wep.h * 0.5, dmgMul: m, kbMul: m, spd: 1,\n           shakeMul: 1.4, stopMul: 1.35, sfxMul: 1.5, sparkMul: 1,\n           seedDepth: 1, grip: 0.5, wear: 0.55,\n           special: null, onSwing: null, onHit: null, box: null };\n};\n\nexport const WEAPONS = {\n\n  // ───────────────────────────────────────────────────────────────────────\n  fists: {\n    id: 'fists', name: 'FISTS', cost: 0, free: true, tag: 'STARTER',\n    desc: 'Five beats, full reach on none of them. The only loadout with every drawn frame.',\n    // DISTINCT VERB: the only weapon with no reach bonus and no hitstop tax —\n    // the fastest recovery in the game, so it is the only loadout where the\n    // five-beat string actually loops inside the 0.45s combo window.\n    body: null,\n    art: null,\n    stats: { ...FIST_STATS },\n    skills: [\n      { id:'fists.uppercut', name:'UPPERCUT', secret:false,\n        hint:'Launch 30 things clean off the floor with beat 4.',\n        req: c => (c['fists.launch']||0) >= 30,\n        mod: { beat4Seed: 2 } },\n      { id:'fists.counter', name:'COUNTER', secret:true,\n        hint:'???',\n        real:'COUNTER — swing within 0.35s of a dodge for 2.2× damage.',\n        req: c => (c['fists.counter']||0) >= 8,\n        mod: { counterMul: 2.2 } },\n    ],\n  },\n\n  // ───────────────────────────────────────────────────────────────────────\n  chair: {\n    id: 'chair', name: 'OFFICE CHAIR', cost: 6000, tag: 'CROWD',\n    desc: 'Wide, clumsy, and once you learn to spin it, it hits what is behind you.',\n    // DISTINCT VERB: the ONLY weapon whose hitbox is not in front of the\n    // player. SPIN makes `box` symmetric about p.cx and suppresses the lunge —\n    // a geometry change nothing else in the game has.\n    body: { w:28, h:34, mass:1.6, color:'#5f6a86', label:'CHAIR' },\n    art: 'chair',\n    stats: {\n      reach: 32, hh: 18, dmgMul: 2.0, kbMul: 2.2, spd: 0.95,\n      shakeMul: 1.4, stopMul: 1.35, sfxMul: 1.5, sparkMul: 1,\n      seedDepth: 1, grip: 0.45, wear: 0,\n      special: (p, s) => (p.grounded && has(s, 'chair.spin')) ? 'spin' : null,\n      onSwing: null,\n      onHit: (p, b, s, a) => {\n        if (a.hit.size === 3) bump(s, 'chair.multi3');\n        if (Math.abs(b.vx) > 900) bump(s, 'chair.launched');\n      },\n    },\n    skills: [\n      { id:'chair.spin', name:'CHAIR SPIN', secret:false,\n        hint:'Hit three things with one chair swing.',\n        req: c => (c['chair.multi3']||0) >= 1, mod: {} },\n      { id:'chair.cannon', name:'CHAIR CANNON', secret:true,\n        hint:'???',\n        real:'CHAIR CANNON — the spin knocks harder and seeds chains one link deeper.',\n        req: c => (c['chair.launched']||0) >= 12,\n        mod: { kbMul: 2.8, spinSeed: 2 } },\n    ],\n  },\n\n  // ───────────────────────────────────────────────────────────────────────\n  stapler: {\n    id: 'stapler', name: 'STAPLER', cost: 14000, tag: 'CHAIN',\n    desc: 'Almost no knockback. That is the point — things stay where you can chain them.',\n    // DISTINCT VERB: the ONLY weapon that deliberately does NOT scatter.\n    // kbMul 0.30 against the Math.max(0.6, mass*0.5) floor at player.js:164\n    // means a mug (0.4) and a stack (0.5) take 30% of the impulse instead of\n    // 100%, so a desk cluster stays packed inside CHAOS.chainWindow. Paper and\n    // printers get seeded a link deeper on hit. Fastest spd in the game.\n    body: { w:18, h:10, mass:0.8, color:'#c4484c', label:'STAPLER' },\n    art: 'stapler',\n    stats: {\n      reach: 14, hh: 6, dmgMul: 0.85, kbMul: 0.30, spd: 1.45,\n      shakeMul: 0.65, stopMul: 0.5, sfxMul: 0.85, sparkMul: 0.6,\n      seedDepth: 1, grip: 0.5, wear: 0,\n      special: (p, s) => has(s, 'stapler.burst') ? 'burst' : null,\n      onSwing: null,\n      onHit: (p, b, s, a) => {\n        if (b.kind === 'stack' || b.kind === 'printer') {\n          FX.paper(b.cx, b.cy, 6);\n          bump(s, 'stapler.paper');\n        }\n        if (p.hitTimes.length >= 12) bump(s, 'stapler.fast');\n      },\n    },\n    skills: [\n      { id:'stapler.jam', name:'PAPER JAM', secret:false,\n        hint:'Staple 40 sheets of paper.',\n        req: c => (c['stapler.paper']||0) >= 40,\n        mod: { seedDepth: 2 } },\n      { id:'stapler.burst', name:'STAPLE BURST', secret:true,\n        hint:'???',\n        real:'STAPLE BURST — heavy fires three hits inside one active window.',\n        req: c => (c['stapler.fast']||0) >= 4, mod: {} },\n    ],\n  },\n\n  // ───────────────────────────────────────────────────────────────────────\n  pan: {\n    id: 'pan', name: 'FRYING PAN', cost: 26000, tag: 'STUN',\n    desc: 'Nobody it touches gets up quickly, and the boss forgets what he was doing.',\n    // DISTINCT VERB: the ONLY weapon that writes to another body's TIMERS\n    // rather than its hp — Coworker.downT (office.js:88) and Boss.attackCd\n    // (office.js:99). Damage is irrelevant to it; time is its currency.\n    body: { w:32, h:14, mass:1.8, color:'#2f3340', label:'PAN' },\n    art: 'pan',\n    stats: {\n      reach: 28, hh: 16, dmgMul: 1.5, kbMul: 1.1, spd: 0.90,\n      shakeMul: 1.8, stopMul: 2.2, sfxMul: 1.6, sparkMul: 1.3,\n      seedDepth: 1, grip: 0.16, wear: 0,\n      special: (p, s) => has(s, 'pan.clang') ? 'clang' : null,\n      onSwing: null,\n      onHit: (p, b, s, a) => {\n        if (b.type === 'npc') { b.downT += 2.2; bump(s, 'pan.stun'); }\n        if (b.type === 'boss' && b.fighting) {\n          b.attackCd += (a.W.bossStagger || 0.8);\n          if (b.swingT > 0) bump(s, 'pan.parry');   // hit him mid-swing\n        }\n      },\n    },\n    skills: [\n      { id:'pan.clang', name:'CLANG', secret:false,\n        hint:'Put 20 coworkers on the floor.',\n        req: c => (c['pan.stun']||0) >= 20, mod: {} },\n      { id:'pan.deflect', name:'DEFLECT', secret:true,\n        hint:'???',\n        real:'DEFLECT — interrupt the boss mid-swing and he stays stunned twice as long.',\n        req: c => (c['pan.parry']||0) >= 6,\n        mod: { bossStagger: 1.8 } },\n    ],\n  },\n\n  // ───────────────────────────────────────────────────────────────────────\n  hammer: {\n    id: 'hammer', name: 'HAMMER', cost: 48000, tag: 'DEMOLITION',\n    desc: 'Slow enough to be a decision. The only thing in the office that breaks the office.',\n    // DISTINCT VERB: the ONLY weapon that acts on WORLD GEOMETRY. The air\n    // heavy is a ground slam with a radial hitbox that ignores facing, and\n    // with DEMOLITION it splices a desk out of S.world.statics — removing the\n    // collider and the drawing in one line, because main.js:435 renders desks\n    // straight from that array.\n    body: { w:30, h:12, mass:2.4, color:'#8a8f9e', label:'HAMMER' },\n    art: 'hammer',\n    stats: {\n      reach: 36, hh: 20, dmgMul: 3.2, kbMul: 2.4, spd: 0.78,\n      shakeMul: 1.9, stopMul: 1.5, sfxMul: 1.7, sparkMul: 1.4,\n      seedDepth: 1, grip: 0.20, wear: 0,\n      special: (p, s) => (!p.grounded && has(s, 'hammer.slam')) ? 'slam' : null,\n      onSwing: (p, s, a) => {\n        if (a.sp === 'slam') { p.vy = 900; p.slamPending = true; p.atk = null; p.state = 'air'; }\n      },\n      onHit: (p, b, s, a) => { if (b.broken) bump(s, 'hammer.broke'); },\n    },\n    skills: [\n      { id:'hammer.slam', name:'GROUND SLAM', secret:false,\n        hint:'Break 30 objects with the hammer.',\n        req: c => (c['hammer.broke']||0) >= 30, mod: {} },\n      { id:'hammer.demolition', name:'DEMOLITION', secret:true,\n        hint:'???',\n        real:'DEMOLITION — a ground slam onto a desk destroys the desk.',\n        req: c => (c['hammer.deskSlam']||0) >= 3,\n        mod: { breaksDesks: true } },\n    ],\n  },\n\n  // ───────────────────────────────────────────────────────────────────────\n  rocketchair: {\n    id: 'rocketchair', name: 'ROCKET OFFICE CHAIR', cost: 90000, tag: 'MOVEMENT',\n    desc: 'Duct tape, two boosters, no plan. The heavy button is not an attack any more.',\n    // DISTINCT VERB: the ONLY item that replaces LOCOMOTION. The grounded\n    // heavy cancels the attack outright and hands control to a ride block that\n    // drives vx and runs a continuous hitbox. Nothing else in the game takes\n    // over movement except the dodge.\n    body: { w:32, h:36, mass:2.0, color:'#5f6a86', label:'ROCKET' },\n    art: 'rocketchair',\n    stats: {\n      reach: 28, hh: 22, dmgMul: 1.4, kbMul: 1.9, spd: 1.10,\n      shakeMul: 1.5, stopMul: 0.9, sfxMul: 1.5, sparkMul: 1.6,\n      seedDepth: 1, grip: 0.5, wear: 0,\n      special: (p, s) => p.grounded ? 'launch' : 'boost',\n      onSwing: (p, s, a) => {\n        if (a.sp === 'launch') {\n          p.rideT = RIDE.time; p.rideX0 = p.cx; p.rideHit = null;\n          p.atk = null; p.state = 'ride'; SFX.throw_(); FX.kick(6, 0.03);\n        }\n        if (a.sp === 'boost') { p.vy = -520; p.vx += p.face * 280; }\n      },\n      onHit: null,\n    },\n    skills: [\n      { id:'rocket.steer', name:'STEERING', secret:false,\n        hint:'Cover 600px in a single launch.',\n        req: c => (c['rocket.dist']||0) >= 600,\n        mod: { rideSteer: true } },\n      { id:'rocket.resign', name:'RESIGNATION', secret:true,\n        hint:'???',\n        real:'RESIGNATION — ride into the boss and he goes down where he stands.',\n        req: c => (c['rocket.boss']||0) >= 1,\n        mod: { rideBossDown: true } },\n    ],\n  },\n};\n\nexport const RIDE = { time: 1.15, speed: 900, hitEvery: 0.22 };\n\n// ── the one resolver ───────────────────────────────────────────────────────\n// NEVER Object.assign onto row.stats — that would mutate the module literal\n// permanently, which is the exact hazard the map flags for ATTACK.\nexport function statsFor(wep, s) {\n  if (!wep) return FIST_STATS;\n  if (!wep.wepId) return propStats(wep);\n  const row = WEAPONS[wep.wepId];\n  if (!row) return propStats(wep);\n  const mods = row.skills.filter(sk => s.career.skills.includes(sk.id)).map(sk => sk.mod);\n  return Object.assign({}, row.stats, ...mods);   // fresh object every attack\n}\n\nexport function makeWeapon(id, x, y) {\n  const row = WEAPONS[id];\n  if (!row || !row.body) return null;\n  const b = new Body({\n    x, y, w: row.body.w, h: row.body.h, mass: row.body.mass,\n    hp: 1e9, value: 0, type: 'weapon',      // NOT 'prop' — invisible to every\n    kind: row.art, bounce: 0.2,             // main.js loop that filters on it\n    grabbable: false,\n    extra: { wepId: id, label: row.body.label, color: row.body.color },\n  });\n  b.held = true;\n  return b;\n}\n```\n\n**Why each is mechanically distinct, stated plainly:** chair changes hitbox *geometry* (behind you); stapler changes *knockback direction economics* (it packs instead of scattering, which is the only way to build long chains); pan writes to *other bodies' timers* instead of hp; hammer acts on *world statics*; rocket chair replaces *locomotion*; fists are the only loadout with zero hitstop tax and the full drawn frame set.\n\n---\n\n## 2. THE SAVE BLOB — `js/save.js` (NEW FILE)\n\nKey: **`we.career`** (unversioned key, version *inside* the payload). `character.js:50` bumps `we.look.v2` by renaming, which orphans the old blob — fine for a look, unacceptable for earned currency. It must **not** share `we.look.v2`: `loadLook` (character.js:56-59) does `Object.assign(defaultLook(), JSON.parse(raw))`, so career fields would be folded into `S.look` and re-persisted by every swatch click (main.js:170).\n\n```js\n// js/save.js — the career. localStorage key 'we.career'.\nconst KEY = 'we.career';\nconst SCHEMA = 1;\n\nexport const defaultCareer = () => ({\n  v: SCHEMA,\n  bank: 0,          // spendable\n  lifetime: 0,      // never debited — CAREER EARNINGS\n  spent: 0,\n  shifts: 0,\n  owned: ['fists'],       // STRING ids. Never indices.\n  equipped: 'fists',\n  skills: [],             // ['chair.spin', ...]\n  counters: {},           // { 'hammer.broke': 12, ... }\n  best: { chain: 0, damage: 0, score: 0, salary: 0 },\n});\n\nconst MIGRATIONS = {\n  // 1: c => { c.prestige = 0; c.v = 2; return c; }\n};\n\nexport function loadCareer() {\n  const d = defaultCareer();\n  let raw;\n  try { raw = localStorage.getItem(KEY); } catch (e) { return d; }\n  if (!raw) return d;\n  let c;\n  try { c = JSON.parse(raw); } catch (e) { return d; }\n  if (!c || typeof c !== 'object') return d;\n\n  // Object.assign is SHALLOW — merge the two nested objects explicitly or a\n  // new field inside `best`/`counters` comes back undefined and NaNs on ++.\n  const out = Object.assign(d, c);\n  out.counters = Object.assign({}, c.counters || {});\n  out.best     = Object.assign(defaultCareer().best, c.best || {});\n\n  // Forward migration only. A save from a NEWER build keeps its own v and is\n  // left alone — never stamp c.v = SCHEMA unconditionally.\n  while (out.v < SCHEMA && MIGRATIONS[out.v]) Object.assign(out, MIGRATIONS[out.v](out));\n  if (out.v < SCHEMA) out.v = SCHEMA;\n\n  // hard invariants — repaired on load, so no table edit can ever soft-lock\n  if (!Array.isArray(out.owned)) out.owned = ['fists'];\n  out.owned = out.owned.filter(id => WEAPON_IDS.has(id));\n  if (!out.owned.includes('fists')) out.owned.unshift('fists');\n  if (!out.owned.includes(out.equipped)) out.equipped = 'fists';\n  if (!Array.isArray(out.skills)) out.skills = [];\n  out.bank     = Math.max(0, Math.floor(+out.bank) || 0);\n  out.lifetime = Math.max(0, Math.floor(+out.lifetime) || 0);\n  return out;\n}\n\nlet warned = false;\nexport function saveCareer(c, s) {\n  try { localStorage.setItem(KEY, JSON.stringify(c)); return true; }\n  catch (e) {\n    if (!warned && s) { warned = true; s.toast('PROGRESS IS NOT SAVING', 'boss'); }\n    return false;                     // a lost look is fine; a lost wallet is not\n  }\n}\n```\n\n`WEAPON_IDS` is `new Set(Object.keys(WEAPONS))`, imported from `weapons.js`. A live blob after seven shifts:\n\n```json\n{ \"v\":1, \"bank\":18420, \"lifetime\":96310, \"spent\":20000, \"shifts\":7,\n  \"owned\":[\"fists\",\"chair\",\"stapler\"], \"equipped\":\"stapler\",\n  \"skills\":[\"chair.spin\",\"stapler.jam\"],\n  \"counters\":{\"chair.multi3\":4,\"chair.launched\":7,\"stapler.paper\":51,\n              \"stapler.fast\":1,\"fists.launch\":18,\"fists.counter\":2,\n              \"pan.stun\":0,\"hammer.broke\":0,\"rocket.dist\":0},\n  \"best\":{\"chain\":6,\"damage\":41200,\"score\":88410,\"salary\":9740} }\n```\n\n---\n\n## 3. FILE-BY-FILE CHANGES, IN DEPENDENCY ORDER\n\n### 3.1 `js/config.js` — additive only\n\nAppend beside `ATTACK` (do **not** edit the existing rows):\n\n```js\n// Synthetic move rows. Never compared to ATTACK.heavy by identity, so they\n// deliberately get no lunge and small sparks — correct for both.\nATTACK.slam = { startup:0, active:0.10, recover:0.22, dmg:34, kbX:520, kbY:-560,\n                reach:150, hh:70, hitstop:0.150, shake:14, pose:'heavy' };\nATTACK.ride = { startup:0, active:0.10, recover:0,    dmg:14, kbX:640, kbY:-300,\n                reach:52,  hh:44, hitstop:0.020, shake:2,  pose:'swing' };\n\nexport const WEAPON = {\n  hitstopMax: 0.30,     // FX.kick is a global Math.max that freezes update()\n  attach: 'hand',       // 'hand' | 'fixed'  — see §4 fallback\n};\n\nexport const WAGE = {\n  perSec:    26,\n  cleanRamp: 60,        // seconds of not breaking anything per +1x\n  cleanMax:  2.6,\n  clockIn:   400,\n  softAfter: 300,       // salary decays past here...\n  softFloor: 0.20,      // ...to this floor. No AFK farm.\n};\n```\n\n**Replace** `QUIET_RANKS` (lines 85-90) — the old 0/250/700/1400 thresholds were sized for HR-only income and a wage clears 1400 in 20 seconds:\n\n```js\nexport const QUIET_RANKS = [\n  { at: 0,     name: 'MODEL EMPLOYEE' },\n  { at: 3500,  name: 'QUIET QUITTER' },\n  { at: 7500,  name: 'PROFESSIONAL LOITERER' },\n  { at: 13000, name: 'PAID TO EXIST' },\n];\n```\n\n### 3.2 `js/chaos.js` — two edits\n\nLine 35, the payout. Depth 1 is a seed, not a payday:\n\n```js\n    const coins = Math.round(CHAOS.coinBase * Math.max(0, depth - 1) * this.s.chaosMul);\n    if (coins > 0) { this.pending += coins; this.s.earn(coins, 'chaos'); }\n```\n\nThat is the whole fix for the broken-prop farm: `damageBody` already early-returns on `b.broken` (main.js:76), and now the surviving `ignite` at depth 1 pays nothing, so mashing J at a dead chair earns zero. Chains are untouched — `onImpact` (chaos.js:61) always ignites at `src.chainDepth + 1`, which is ≥ 2.\n\n`this.s.coins += coins` is replaced by `this.s.earn(...)` — see 3.5.\n\n### 3.3 `js/events.js` — one edit\n\nLine 33: `s.coins += Math.round(38 * dt)` → `s.earn(38 * dt, 'salary')`. (`earn` carries the fractional remainder, so 38/s is paid accurately instead of rounding to 0 or 1 per frame.)\n\n### 3.4 `js/save.js`, `js/weapons.js` — new, as written above.\n\n### 3.5 `js/main.js`\n\n**(a) Imports** — add to the block at lines 3-15:\n```js\nimport { WEAPON, WAGE } from './config.js';           // extend existing import\nimport { loadCareer, saveCareer } from './save.js';\nimport { WEAPONS, makeWeapon, statsFor, checkUnlocks, bump, has } from './weapons.js';\n```\n\n**(b) The `S` literal (lines 27-53)** — add beside `look:` at line 39. These sit in the literal, which is built once at module eval; they must stay **off** the `startShift` whitelist:\n```js\n  career: loadCareer(),\n  careerDirty: false,\n  _frac: 0,\n```\nAnd these per-shift counters, which **do** go on the whitelist:\n```js\n  chaosCoins: 0, salary: 0, cleanT: 0, clockedIn: false,\n```\n\n**(c) `S.earn(n, src)`** — new method beside `addAnger` (line 56) / `damageBody` (line 67). Banking is continuous, not at `endShift`. This closes all four coin-loss holes at once: the R key at line 735 restarts without ever calling `endShift`; `endShift` has no re-entry guard and three call sites (bossDown's 2600ms timer at 286, `#btnEnd` at 723, Escape at 736); a browser refresh has no path back to the title; and alt-tab mid-shift loses nothing.\n\n```js\n  earn(n, src) {\n    if (!(n > 0)) return 0;\n    this._frac += n;\n    const whole = Math.floor(this._frac);\n    if (whole <= 0) return 0;\n    this._frac -= whole;\n    this.coins += whole;                                  // per-shift, HUD + report\n    if (src === 'chaos') this.chaosCoins += whole; else this.salary += whole;\n    this.career.bank += whole;\n    this.career.lifetime += whole;\n    this.careerDirty = true;                              // flushed on a 2s debounce\n    return whole;\n  },\n```\n\nFlush loop, in `update()`:\n```js\n  S.saveT = (S.saveT || 0) + dt;\n  if (S.careerDirty && S.saveT > 2) { S.saveT = 0; S.careerDirty = false; saveCareer(S.career, S); }\n```\nPlus `addEventListener('pagehide', () => { if (S.careerDirty) saveCareer(S.career, S); })` and the same on `visibilitychange` when hidden, in the WIRING block.\n\n**(d) `S.damageBody`** — one line inside the destruction block (after `this.destroyed++`, line 81): `this.cleanT = 0;`. Nothing else in that function changes.\n\n**(e) `startShift()` (lines 219-250)**\n- Add to the reset block after line 228: `S.chaosCoins = 0; S.salary = 0; S.cleanT = 0; S.clockedIn = false; S._frac = 0;`\n- **Do not** touch `S.career`.\n- After `buildOffice(S.world, S)` at line 240, add: `equipShiftWeapon();`\n- Line 245: add `hide('shop');` to the teardown list. This is the single highest-risk omission in the whole change — `.screen` is `position:absolute; inset:0; background:#0a0b0e; z-index:20` (css:74), so a shop left mounted is an opaque panel over a running game and reads as a hang.\n- Increment `S.career.shifts++` and pay clock-in: `S.earn(WAGE.clockIn, 'salary')`.\n\n```js\nfunction equipShiftWeapon() {\n  const p = S.player;\n  p.equipped = null;\n  const id = S.career.equipped;\n  if (!id || id === 'fists' || !S.career.owned.includes(id)) return;\n  p.equipped = makeWeapon(id, p.cx, p.cy);   // NOT added to S.world.bodies\n  checkUnlocks(S);\n}\n```\n\n**(f) The salary tick** — in `update()`, right after `S.time += dt`:\n```js\n  S.cleanT += dt;\n  const clean = 1 + Math.min(WAGE.cleanMax - 1, S.cleanT / WAGE.cleanRamp);\n  const decay = S.shiftT < WAGE.softAfter ? 1\n              : Math.max(WAGE.softFloor, 1 - (S.shiftT - WAGE.softAfter) / 600);\n  S.earn(WAGE.perSec * clean * decay * dt, 'salary');\n```\n\n**(g) `update()` — weapon pose** — after each of the two `S.player.carryPose()` calls (lines 351, 358) add `S.player.weaponPose();`.\n\n**(h) `endShift()` (289-332)** — no deposit here (banking is continuous). Two required companion edits plus rows:\n- Line 296: `S.coins * 0.5` → `S.chaosCoins * 0.5`. Standing still must not raise CHAOS SCORE.\n- Line 303: `if (S.coins >= r.at)` → `if (S.salary >= r.at)`. The QUIET ladder scores the *wage*, never the bank and never the combined figure.\n- Rows: replace `['COINS EARNED', ...]` with three rows — `['SALARY', S.salary.toLocaleString()]`, `['CHAOS COINS', S.chaosCoins.toLocaleString()]`, `['BANKED', S.career.bank.toLocaleString()]`.\n- Update `S.career.best` and `saveCareer(S.career, S)` once at the end.\n\n**(i) `updateHud()` (392-414)** — add one line: `$('hWeapon').textContent = S.player.carrying ? (S.player.carrying.label || S.player.carrying.kind).toUpperCase() : (S.career.equipped === 'fists' ? 'FISTS' : WEAPONS[S.career.equipped].name);`\n\n**(j) `render()` — the two draw edits**\n1. Props loop (line 466): **unchanged**. The equipped weapon is not in `world.bodies`, so it cannot appear there. A *grabbed* prop keeps drawing exactly as it does today.\n2. Immediately after the player draw block, before the name label at line 541:\n```js\n  if (p.equipped) drawWeapon(ctx, p, pose);   // `pose` = the resolved name, see §4\n```\n3. **Attack-arc tell (546-551)** — it already lies by tens of pixels whenever you swing a grabbed prop. Fix it while we are here:\n```js\n  if (p.atk && p.atk.phase === 'active') {\n    const d = p.atk.d, W = p.atk.W;\n    const reach = d.reach + W.reach, hh = d.hh + W.hh;\n    ctx.fillStyle = 'rgba(255,255,255,.20)';\n    const spin = p.atk.sp === 'spin' || p.atk.sp === 'clang';\n    const bx = spin ? p.cx - reach : (p.face > 0 ? p.x + p.w - 6 : p.x - reach + 6);\n    roundRect(ctx, bx, p.cy - hh / 2, spin ? reach * 2 : reach, hh, 8); ctx.fill();\n  }\n```\n\n**(k) The R key (line 735)** — no change needed. `earn` has already banked everything.\n\n**(l) Shop wiring** — see §3.7.\n\n### 3.6 `js/player.js`\n\n**Constructor (after line 21):**\n```js\n    this.equipped = null;     // owned weapon Body; never in world.bodies\n    this.slamPending = false;\n    this.rideT = 0; this.rideX0 = 0; this.rideA = null; this.rideT2 = 0;\n    this.sinceDodge = 99;\n    this.hitTimes = [];       // rolling 4s window, for STAPLE BURST\n```\n\n**`update()` — four insertions, in this order:**\n\n1. After line 31: `this.sinceDodge = this.dodgeT > 0 ? 0 : this.sinceDodge + dt;` and trim the hit window: `const hz = s.time - 4; while (this.hitTimes.length && this.hitTimes[0] < hz) this.hitTimes.shift();`\n\n2. Inside the landing block (lines 39-43), after `SFX.land(...)`:\n```js\n      if (this.slamPending) { this.slamPending = false; slamShock(this, s); }\n```\n\n3. **The ride block, placed immediately after the dodge block (after line 64) and therefore BEFORE `if (this.atk)` at line 67.** This resolves the placement problem cleanly because the launch sets `p.atk = null` in `onSwing` — the heavy is *consumed* by the launch, so nothing is left running underneath:\n```js\n    if (this.rideT > 0) {\n      this.rideT -= dt;\n      const W = statsFor(this.equipped, s);\n      if (W.rideSteer && IN.axis) this.face = Math.sign(IN.axis);\n      this.vx += (this.face * RIDE.speed - this.vx) * Math.min(1, dt * 9);\n      this.state = 'ride';\n      this.rideT2 += dt;\n      if (!this.rideA) this.rideA = makeRideAtk(W);\n      if (this.rideT2 > RIDE.hitEvery) { this.rideT2 = 0; this.rideA.hit.clear(); }\n      this._swing(ATTACK.ride, s, this.rideA);\n      FX.spark(this.cx - this.face * 18, this.cy + 14, 2, '#ffb15e', 120);\n      if (this.rideT <= 0) {\n        setMax(s, 'rocket.dist', Math.abs(this.cx - this.rideX0));\n        this.rideA = null; this.state = 'idle';\n      }\n      return;\n    }\n```\n`makeRideAtk(W)` returns a **fully specified** synthetic attack — every field `_swing` reads must exist or it throws or NaNs:\n```js\nconst makeRideAtk = (W) => ({ kind:'ride', step:0, phase:'active', t:0,\n  hit:new Set(), wep:null, W, d:ATTACK.ride, sp:'ride' });\n```\n900 px/s at dt 0.0167 is 15 px/frame against 120 px-wide desk colliders — no tunnelling. `_vsStatic`'s side-hit threshold (200 vx) fires normally, and level bounds clamp at engine.js:91-93.\n\n**`_startAttack` — replace lines 109-119 entirely:**\n```js\n  _startAttack(kind, step, s) {\n    const wep = this.carrying || this.equipped;\n    const W = statsFor(wep, s);\n    const d = kind === 'heavy' ? ATTACK.heavy : ATTACK.light[step];\n    const sp = W.special ? W.special(this, s) : null;\n    const counter = W.counterMul && this.sinceDodge < 0.35;\n    if (counter) bump(s, 'fists.counter');\n    this.atk = { kind, step, phase:'startup', t:0, hit:new Set(), wep, W, d, sp, counter };\n    SFX.whiff();\n    this.state = 'attack';\n    if (IN.axis) this.face = Math.sign(IN.axis);\n    if (W.onSwing) W.onSwing(this, s, this.atk);\n  }\n```\nCall sites at lines 73 and 76 gain the `s` argument. Note `_startAttack` has no `s` in scope today (`this.s` is set at line 30) — passing it explicitly is cleaner than relying on assignment order.\n\n**`_stepAttack` — replace line 123-124:**\n```js\n    const d = a.d;                 // SNAPSHOT, not a live re-read of ATTACK\n    a.t += dt * a.W.spd;\n```\n`a.d` is a reference to the same `ATTACK.light[step]` / `ATTACK.heavy` object, never a clone — so `d === ATTACK.heavy` at :152/:173/:175 still holds. Snapshotting is a provable no-op today and it closes the class of bug where a weapon changing mid-attack moves the phase boundaries under a running timer.\n\nLine 134 gains a per-weapon window: `this.comboTimer = ATTACK.comboWindow * (a.W.comboMul || 1);`\n\n**`_swing` — replace lines 142-145 and 174-180:**\n```js\n    const wep = a.wep && !a.wep.dead ? a.wep : null;\n    const W = a.W;\n    const reach = d.reach + W.reach;\n    const hh = d.hh + W.hh;\n    let mulD = W.dmgMul, mulK = W.kbMul;\n    if (a.counter) mulD *= W.counterMul;\n    if (a.step === 3 && W.beat4Seed) a.seed = W.beat4Seed;\n    const spin = a.sp === 'spin' || a.sp === 'clang';\n    if (spin) { mulD *= (a.sp === 'clang' ? 0 : 1); }\n    const box = spin\n      ? { x: this.cx - reach, y: this.cy - hh / 2, w: reach * 2, h: hh }\n      : { x: this.face > 0 ? this.x + this.w - 6 : this.x - reach + 6,\n          y: this.cy - hh / 2, w: reach, h: hh };\n    if (!spin && a.phase === 'active' && (d === ATTACK.heavy || a.step >= 3)) {\n      this.vx += this.face * 520 * (1 / 60);\n    }\n```\nInside the loop, the knockback uses `mulK`, the damage uses `mulD`, and the ignite becomes a single seeded call (never two):\n```js\n      s.damageBody(b, d.dmg * mulD, this);\n      s.chaos.ignite(b, Math.max(a.seed || W.seedDepth, b.chainDepth), b.label || b.kind);\n      if (a.step === 3 && b.vy < -200) bump(s, 'fists.launch');\n      if (W.onHit) W.onHit(this, b, s, a);\n```\nFX/SFX become scalars instead of the `wep ? x : 1` hard switches, with the hitstop clamped because `FX.kick` is a global `Math.max` that stops `update()` entirely (fx.js:12-15, main.js:680-684) — a pan at `stopMul 2.2` would otherwise be a 0.36 s full-game freeze per hit:\n```js\n      FX.spark(hx, hy, Math.round((d === ATTACK.heavy ? 16 : 9) * W.sparkMul), '#fff',\n               d === ATTACK.heavy ? 420 : 260);\n      FX.kick(d.shake * W.shakeMul,\n              Math.min(WEAPON.hitstopMax, d.hitstop * W.stopMul));\n      SFX.hit(Math.min(1, (d === ATTACK.heavy ? 1 : 0.35 + a.step * 0.2) * W.sfxMul));\n      if (wep && W.wear > 0) {          // grabbed props only; W.wear is 0 for\n        s.damageBody(wep, d.dmg * W.wear, this);   // every owned weapon\n        wep.flash = 0.12;\n        if (wep.broken) { wep.held = false; this.carrying = null; a.wep = null; }\n      }\n      this.hitTimes.push(s.time);\n      s.hits++;\n```\n`wep.held = false` fixes the pre-existing orphan-body bug: today a prop that breaks mid-swing keeps `held:true`, is skipped by `World.step` integration (engine.js:59) and by the dead sweep (109), and freezes in mid-air forever, still drawing.\n\n**STAPLE BURST** — in `_stepAttack`, inside the `active` branch: `if (a.sp === 'burst' && a.t > d.active * 0.33 * (a.burstN = (a.burstN||0) + 0) ) {}` — concretely, clear the dedupe set twice through the window:\n```js\n    } else if (a.phase === 'active') {\n      if (a.sp === 'burst') {\n        const n = Math.floor(a.t / (d.active / 3));\n        if (n > (a.burstN || 0)) { a.burstN = n; a.hit.clear(); }\n      }\n      if (a.sp === 'spin' && a.t > d.active * 0.5 && !a.spun) { a.spun = true; a.hit.clear(); }\n      this._swing(d, s, a);\n```\n\n**`slamShock`** — module function in `player.js`, called from the landing block. It runs from `Player.update`, i.e. **before** `World.step`, so splicing `S.world.statics` is safe (never mid-iteration):\n```js\nfunction slamShock(p, s) {\n  const W = statsFor(p.equipped, s);\n  const a = { kind:'slam', step:0, phase:'active', t:0, hit:new Set(),\n              wep:p.equipped, W, d:ATTACK.slam, sp:'slam' };\n  FX.kick(14, Math.min(WEAPON.hitstopMax, 0.15));\n  FX.spark(p.cx, p.y + p.h, 24, '#fff', 520);\n  SFX.smash('metal', 1);\n  p._swing(ATTACK.slam, s, a);            // radial: see box override below\n  const box = { x: p.cx - 150, y: p.y + p.h - 20, w: 300, h: 40 };\n  for (let i = s.world.statics.length - 1; i >= 0; i--) {\n    const d = s.world.statics[i];\n    if (!d.oneWay || !rectsOverlap(box, d)) continue;\n    bump(s, 'hammer.deskSlam');\n    if (W.breaksDesks) {\n      s.world.statics.splice(i, 1);       // removes collider AND drawing (main.js:435)\n      FX.debris(d.x + d.w / 2, d.y, 20, '#6a7086');\n      s.damage += 900; s.destroyed++; s.addAnger(6);\n    }\n  }\n}\n```\nFor the slam, `_swing` uses `a.sp === 'slam'` to take a symmetric box like `spin` does.\n\n**Rocket ride hit hook** — the ride's `onHit` equivalent lives in `_swing` via `W.onHit`; add to the rocketchair stats an `onHit` that bumps `rocket.boss` and, with `rideBossDown`, calls `b.hp = 0`-equivalent via `s.damageBody(b, 400, p)` when `b.type === 'boss' && b.fighting`.\n\n### 3.7 `index.html`\n\n- HUD `.hud-left` (line 26), a fourth stat mirroring `#hCoins`:\n  `<div class=\"stat\"><span class=\"k\">WEAPON</span><span class=\"v\" id=\"hWeapon\">FISTS</span></div>`\n- Line 94, give the already-present placeholder an id — it becomes the bank display with zero layout work:\n  `<div class=\"prow\"><span>CAREER EARNINGS</span><b id=\"pBank\">$0</b></div>`\n- Report (after line 148): `<button id=\"btnShop\" class=\"ghost-btn wide\">SUPPLY CLOSET</button>`\n- Title (beside `#btnHelp`, line 72): the same button.\n- New screen **before** `#rotate` (line 154):\n```html\n<div id=\"shop\" class=\"screen hidden\">\n  <div class=\"screen-inner create-inner\">\n    <h2>SUPPLY CLOSET</h2>\n    <div class=\"shop-bank\">BANK <b id=\"shopBank\">0</b></div>\n    <div id=\"weaponRows\"></div>\n    <button id=\"btnShopBack\" class=\"big-btn\">BACK</button>\n  </div>\n</div>\n```\n\n### 3.8 `css/game.css` — additive\n\n```css\n.shop-bank{font-size:11px;letter-spacing:.18em;color:#8d93a6;margin-bottom:14px}\n.shop-bank b{color:#ffd75e;font-size:16px}\n.wcard{display:block;width:100%;text-align:left;margin-bottom:8px;padding:11px 12px;\n  border:2px solid rgba(255,255,255,.14);border-radius:8px;background:rgba(255,255,255,.05);\n  color:#c6ccdd;cursor:pointer;font:inherit}\n.wcard b{display:block;font-size:13px;letter-spacing:.08em;font-weight:900;color:#e8eaf0}\n.wcard .wdesc{display:block;font-size:10.5px;line-height:1.5;color:#8d93a6;margin-top:3px}\n.wcard .wprice{float:right;font-size:11px;font-weight:800;color:#6fb87a}\n.wcard.locked{opacity:.62}\n.wcard.locked .wprice{color:#8d93a6}\n.wcard.on{border-color:#ffd75e;box-shadow:0 0 0 2px rgba(255,215,94,.25)}\n.wskills{display:flex;flex-wrap:wrap;gap:5px;margin-top:7px}\n```\nGold `#ffd75e` is reserved for the **equipped** card only — it means \"selected\" everywhere else in this game (`.big-btn`, `.sw.on`, `#hCoins`, chaos). Locked uses muted `#8d93a6`; affordable prices use `#6fb87a`, already in the palette. Skill chips reuse the existing `.soon` badge (css:156).\n\n### 3.9 `js/main.js` — the shop UI\n\nModelled line-for-line on `buildOptionRows` (141-178) and `openCreator` (209-214).\n\n```js\nfunction openShop(from) {\n  hide(from); show('shop'); buildWeaponRows();\n}\n\nfunction buildWeaponRows() {\n  $('shopBank').textContent = S.career.bank.toLocaleString();\n  const wrap = $('weaponRows'); wrap.innerHTML = '';\n  for (const [id, w] of Object.entries(WEAPONS)) {\n    const owned = S.career.owned.includes(id);\n    const equipped = S.career.equipped === id;\n    const afford = S.career.bank >= w.cost;\n    const b = document.createElement('button');\n    // .wcard, NOT .sw — refreshSwatches (main.js:180-183) queries\n    // document.querySelectorAll('.sw') UNSCOPED against S.look and would strip\n    // .on off every shop card on any creator click or RANDOMISE press.\n    b.className = 'wcard' + (equipped ? ' on' : '') + (!owned && !afford ? ' locked' : '');\n    const price = equipped ? 'EQUIPPED'\n                : owned    ? 'EQUIP'\n                : afford   ? 'BUY — ' + w.cost.toLocaleString()\n                : 'SHORT BY ' + (w.cost - S.career.bank).toLocaleString();\n    const chips = w.skills.map(sk => {\n      const got = S.career.skills.includes(sk.id);\n      const label = got ? sk.name : (sk.secret ? '???' : sk.name);\n      const tip   = got ? (sk.real || sk.hint) : sk.hint;\n      return `<span class=\"soon\" title=\"${tip}\">${label}${got ? ' ✓' : ''}</span>`;\n    }).join('');\n    b.innerHTML = `<span class=\"wprice\">${price}</span><b>${w.name}</b>` +\n                  `<span class=\"wdesc\">${w.desc}</span><span class=\"wskills\">${chips}</span>`;\n    b.onclick = () => {\n      SFX.ui();                       // the module-init pointerdown -> SFX.resume\n                                      // loop (main.js:710) never sees dynamic buttons\n      if (owned) { S.career.equipped = id; }\n      else if (afford) {\n        if (S.career.owned.includes(id)) return;       // idempotent: double-tap safe\n        S.career.bank -= w.cost; S.career.spent += w.cost;\n        S.career.owned.push(id); S.career.equipped = id;\n        toast(w.name + ' — PURCHASED');\n      } else return;\n      saveCareer(S.career, S);\n      checkUnlocks(S);\n      buildWeaponRows();\n    };\n    wrap.appendChild(b);\n  }\n}\n```\nWiring: `$('btnShop').onclick = () => { SFX.ui(); openShop($('report').classList.contains('hidden') ? 'title' : 'report'); };` and `$('btnShopBack').onclick` returns to whichever screen it came from. `openCreator` gains `$('pBank').textContent = '$' + S.career.lifetime.toLocaleString();`.\n\n`frame()` (675-678) needs **no** change — the shop has no animated canvas, deliberately. A shop preview would need its own line beside the `#create`/`drawPreview` check and its failure mode is a silently blank canvas.\n\n---\n\n## 4. HOW THE WEAPON ATTACHES TO THE HAND\n\n### The anchor table\n\nThere is no hand data anywhere: `assets/player/base/anchors.json` has only `frameW 170`, `frameH 137`, `groundY 130.4`, `centreX 66.0`, `standingH 124`, `poses[28]`. And `tools/cutout.py` **overwrites** that file, so hand data must not live in it. The table lives in `js/weapons.js`, in the same source-frame pixel space as `centreX`/`groundY`:\n\n```js\n// Front-hand grip point per pose, in source-frame px (170x137 canvas, +x = the\n// direction the unflipped figure faces), plus the weapon's angle in radians.\n// Calibrate with the H-key overlay (below), not by guessing twice.\nexport const HANDS = {\n  idle:       { x: 88, y: 74, a: 0.10 },\n  idle2:      { x: 88, y: 73, a: 0.10 },\n  carry:      { x: 92, y: 70, a: 0.00 },\n  'run-1':    { x: 92, y: 72, a: 0.25 },\n  'run-2':    { x: 86, y: 70, a: 0.15 },\n  'run-3':    { x: 94, y: 73, a: 0.30 },\n  'run-4':    { x: 84, y: 70, a: 0.10 },\n  'run-5':    { x: 95, y: 74, a: 0.35 },\n  'run-6':    { x: 83, y: 71, a: 0.05 },\n  'jump-up':  { x: 90, y: 64, a: -0.35 },\n  'jump-apex':{ x: 92, y: 66, a: -0.20 },\n  fall:       { x: 90, y: 70, a:  0.20 },\n  land:       { x: 88, y: 80, a:  0.45 },\n  dodge:      { x: 82, y: 82, a:  0.60 },\n  hurt:       { x: 78, y: 76, a: -0.50 },\n  'c1-wind':  { x: 76, y: 72, a: -0.90 },\n  'c1-hit':   { x:104, y: 72, a:  0.15 },\n  'c2-hit':   { x:106, y: 70, a:  0.20 },\n  'c3-hit':   { x:108, y: 66, a: -0.30 },\n  'c4-hit':   { x:100, y: 56, a: -1.10 },\n  'c5-wind':  { x: 74, y: 74, a: -1.20 },\n  'c5-hit':   { x:110, y: 68, a:  1.10 },\n  'heavy-wind':{x: 70, y: 70, a: -1.30 },\n  'heavy-hit':{ x:112, y: 72, a:  1.20 },\n  'air-hit':  { x:106, y: 68, a:  0.60 },\n  swing:      { x:100, y: 70, a:  0.90 },\n  throw:      { x:104, y: 60, a: -0.60 },\n  taunt:      { x: 86, y: 72, a:  0.00 },\n};\n```\n\n### The draw, in `js/art.js` (new export, next to `WORLD.drawProp`)\n\nThe transform mirrors `SPRITES.draw` (art.js:223-238) **exactly** — same `s`, same translate, same flip-before-offset order — so the weapon cannot drift when the player turns, and it uses the identical `p.h * p.squash * 1.06` height, so it stays glued through every landing squash (player.js:40, 46).\n\n```js\nexport function drawWeapon(ctx, p, poseName, wep, time) {\n  const m = SPRITES.meta; if (!m) return;\n  const H = HANDS[poseName] || HANDS.idle;\n  const s = (p.h * p.squash * 1.06) / m.standingH;    // IDENTICAL to the body\n  const flip = p.face < 0;\n\n  ctx.save();\n  ctx.translate(p.cx, p.y + p.h);\n  if (flip) ctx.scale(-1, 1);\n  ctx.translate((H.x - m.centreX) * s, (H.y - m.groundY) * s);\n  ctx.rotate(H.a + (p.atk && p.atk.wep === wep ? swingAngle(p.atk) : 0));\n\n  const im = WORLD.props[wep.kind];\n  if (im) {\n    const meta = WORLD.meta[wep.kind];\n    const ss = (meta && meta.ss) || 2;                 // props.json w/h are\n    const w = im.width / ss, h = im.height / ss;       // SUPERSAMPLED; only ss matters\n    const grip = WEAPONS[wep.wepId].stats.grip;\n    ctx.drawImage(im, -w * grip, -h / 2, w, h);        // pivot at the handle\n  } else {\n    ctx.fillStyle = wep.color || '#6a7086';            // greybox until art lands\n    ctx.fillRect(-wep.w * WEAPONS[wep.wepId].stats.grip, -wep.h / 2, wep.w, wep.h);\n  }\n  ctx.restore();\n}\n\n// A continuous sweep, not the three-position snap carryPose uses. Endpoints\n// match carryPose's -0.7 / +1.1 so nothing reads as a different weapon.\nfunction swingAngle(a) {\n  if (a.phase === 'startup') return -0.7 * Math.min(1, a.t / Math.max(0.001, a.d.startup));\n  if (a.phase === 'active')  return -0.7 + 1.8 * Math.min(1, a.t / Math.max(0.001, a.d.active));\n  return 1.1 - 0.4 * Math.min(1, a.t / Math.max(0.001, a.d.recover));\n}\n```\n\nBecause the sprite is drawn inside the mirrored space, the `scale(-1,1)` handles facing on its own — no `-b.angle` double negation needed, and no `b.angle` at all: the equipped weapon never enters physics, so nothing else writes to it.\n\n### Pose-name resolution — the trap that must not be missed\n\n`SPRITES.draw` silently falls back to `idle` when a frame is missing (art.js:227-228). If the hand lookup does not fall back identically, the weapon is placed for `c4-hit` while the body draws `idle`. Resolve the name **once** in `render()` and pass the same string to both:\n\n```js\n  let pose = poseFor(p, p.animT);\n  if (!SPRITES.has(pose)) pose = 'idle';\n  SPRITES.draw(ctx, pose, p.cx, p.y + p.h, p.h * p.squash * 1.06, p.face < 0, alpha);\n  if (p.equipped) drawWeapon(ctx, p, pose, p.equipped, S.time);\n```\n\n### Calibration and the specific fallback\n\n**Calibration:** add a debug key `H` in the keydown handler that toggles `S.showHands`. When on, `render()` draws a 3px magenta dot at the resolved anchor and prints the pose name. Walk, jump, and hit through all 28 poses once; nudge the table numbers until the dot sits on the knuckles. This is a 20-minute pass, done once, and the values above are a starting point measured off the 170×137 canvas, not a guess to ship.\n\n**The fallback, if it looks wrong:** `WEAPON.attach = 'fixed'` in `config.js` (togglable at runtime by pressing `H` twice). In `'fixed'` mode `drawWeapon` ignores `HANDS` entirely and uses the exact math `carryPose` uses today — `PLAYER.carryOffset {x:26, y:-18}` plus the three-position angle snap `-0.5 / 1.5 / 0.7` and `face * (active ? 1.1 : -0.7)`. That path is already shipping and already looks acceptable for grabbed props, so it is a known-good floor: the weapon rides at a fixed offset from the body rather than in the hand, and nothing else in the build depends on which mode is active. Ship `'hand'`; flip to `'fixed'` if calibration stalls.\n\n**Art pipeline (blocking for the hammer, stapler, pan, rocket chair — `chair.png` already exists):** `tools/pass8-weapons.js` already queues all five renders with the handle-left/business-end-right contract. To land them: add `hammer`, `stapler`, `pan`, `rocketchair` to the `SIZES` dict in `tools/cutout-world.py` (line 98, `if base not in SIZES: continue`, **silently skips unknown filenames with no error**), drop the PNGs into `tools/world/`, run it, and `props.json` gains the entries. Until then every weapon greyboxes to a coloured rect via the `else` branch above — a mismatched look, never a crash.\n\n---\n\n## 5. PRICE CURVE AND THE SOFT-LOCK PROOF\n\n| weapon | cost | first affordable |\n|---|---|---|\n| FISTS | 0 | owned, cannot be un-owned |\n| OFFICE CHAIR | 6,000 | end of shift 1 |\n| STAPLER | 14,000 | shift 2 |\n| FRYING PAN | 26,000 | shift 4 |\n| HAMMER | 48,000 | shift 6–7 |\n| ROCKET OFFICE CHAIR | 90,000 | shift 11–12 |\n\nTotal catalogue: **184,000**, about 17 shifts.\n\n**Soft-lock proof — a player who destroys NOTHING.**\n\nIncome for a zero-destruction shift, all from `S.earn(..., 'salary')`, none of it requiring a single broken object:\n\n- Clock-in: `WAGE.clockIn` = **400**, paid on the first frame of every shift.\n- Wage: `WAGE.perSec` 26/s × clean multiplier. `S.cleanT` only resets inside `S.damageBody`'s destruction block, which a quiet player never reaches, so the multiplier ramps 1.0 → 2.6 over `WAGE.cleanRamp` 60 s and then holds.\n  - Over a 180 s shift: 60 s averaging 1.8× plus 120 s at 2.6× = 26 × (108 + 312) = **10,920**.\n- HR INSPECTION (events.js:33, `dur: 9`, 38/s): **~340** per occurrence, roughly once or twice a shift.\n\n**Zero-destruction total: ≈ 11,600 per 180 s shift.** The 6,000 chair is affordable at the **first** report. Two shifts clear the stapler. Nothing in the ladder is gated behind destruction.\n\nCorner cases:\n- A player who ends a shift after 20 seconds still banks 400 + ~600 = ~1,000; the bar under the report rank (`NEXT: OFFICE CHAIR — 6,000 (5,000 to go)`) moves after *every* shift, with no exceptions, because clock-in alone guarantees it.\n- AFK farming is bounded: past `WAGE.softAfter` 300 s the wage decays to a `softFloor` 0.20 multiple, so an idle player earns ~5/s and would need three hours for a chair.\n- `fists` is `free: true`, is force-restored by `loadCareer`'s invariant repair, and clears the boss's 320 hp unaided (heavy does 38 → 9 connects). No purchase can strand you; equipping is free and reversible; there are no consumables and no sinks other than weapons, so the bank only rises between purchases.\n- `buy()` re-checks `owned.includes(id)` before debiting, so a double-tapped card cannot charge twice.\n- An unknown `equipped` id after a table edit cannot throw: `loadCareer` filters `owned` against `WEAPON_IDS` and resets `equipped` to `'fists'`, and `equipShiftWeapon` returns early on any id not in `owned`.\n\n**Loud shift, for parity:** chains pay `60 × (depth-1)` per link, so a ×6 chain is 60×(1+2+3+4+5) = 900 and a ×3 is 180. A destructive shift making ~25 chains averaging ×3 with a few big ones earns ~6,000 chaos coins, plus a wage held near 1.0–1.3× by constant breakage (~5,500) plus clock-in: **≈ 11,900**. The two playstyles are within 3% of each other, which is the entire point.\n\n---\n\n## 6. SKILLS — EARNED vs SECRET, WITH THEIR EXACT TRIGGERS\n\nTwo per weapon: one visible with its condition printed, one `???`. **Every counter below has exactly one named bump site**, listed. There are no orphan counters.\n\n| weapon | skill | kind | trigger | counter bumped at |\n|---|---|---|---|---|\n| FISTS | UPPERCUT | earned, visible | Launch 30 things off the floor with beat 4 | `fists.launch` — `_swing` loop, `if (a.step === 3 && b.vy < -200)` |\n| FISTS | COUNTER | **secret** | Swing within 0.35 s of a dodge, 8× | `fists.counter` — `_startAttack`, `if (W.counterMul && this.sinceDodge < 0.35)` |\n| CHAIR | CHAIR SPIN | earned, visible | Hit three things with one chair swing | `chair.multi3` — `chair.onHit`, `if (a.hit.size === 3)` |\n| CHAIR | CHAIR CANNON | **secret** | Launch 12 things past 900 px/s | `chair.launched` — `chair.onHit`, `if (Math.abs(b.vx) > 900)` |\n| STAPLER | PAPER JAM | earned, visible | Staple 40 sheets of paper | `stapler.paper` — `stapler.onHit`, `if (b.kind === 'stack' \\|\\| 'printer')` |\n| STAPLER | STAPLE BURST | **secret** | Land 12 hits inside 4 s, four times | `stapler.fast` — `stapler.onHit`, `if (p.hitTimes.length >= 12)` |\n| PAN | CLANG | earned, visible | Put 20 coworkers on the floor | `pan.stun` — `pan.onHit`, `if (b.type === 'npc')` |\n| PAN | DEFLECT | **secret** | Interrupt the boss mid-swing 6× | `pan.parry` — `pan.onHit`, `if (b.type === 'boss' && b.swingT > 0)` |\n| HAMMER | GROUND SLAM | earned, visible | Break 30 objects with the hammer | `hammer.broke` — `hammer.onHit`, `if (b.broken)` |\n| HAMMER | DEMOLITION | **secret** | Ground-slam onto a desk 3× | `hammer.deskSlam` — `slamShock`, per overlapping `oneWay` static |\n| ROCKET | STEERING | earned, visible | Cover 600 px in one launch | `rocket.dist` — `setMax` in the ride block when `rideT <= 0` |\n| ROCKET | RESIGNATION | **secret** | Ride into the boss once | `rocket.boss` — `rocketchair.onHit`, `if (b.type === 'boss')` |\n\nEvery skill is reachable with the weapon it belongs to, and every visible skill's condition is achievable inside one shift. `bump`/`setMax` call `checkUnlocks` immediately, so the toast fires mid-shift and `careerDirty` schedules the write within 2 s. Secret skills render as `???` with a vague hint until unlocked, then swap to their name plus the `real` string on the shop card.\n\n**Gating:** `special()` returns `null` when its skill is not owned. An unmastered weapon still swings correctly and its heavy is the stock `ATTACK.heavy` — no crash path, and mastering the weapon visibly changes what the heavy button does. That is the loop: buy the weapon with coins, earn what it can do by using it. Note the rocket chair's `special` is the one that is **not** skill-gated (`p.grounded ? 'launch' : 'boost'`) — deliberately, because it is the 90,000-coin trophy and must work the moment it is bought.\n\n---\n\n## 7. WHAT NOT TO TOUCH\n\n**Do not edit `js/art.js`'s `poseFor` (256-283).** Specifically leave `if (p.carrying) return 'swing'` (261) and `if (p.carrying) return 'carry'` (274) exactly as they are. They are correct: they describe a *grabbed prop*, and D1 keeps `p.carrying` null while equipped, so all 28 drawn frames — the six-frame run cycle, both idles, all five combo beats, both heavy frames — keep playing. Routing a permanent weapon through `carrying` would delete 15 of 28 authored poses on the player's first purchase, in the renderer main.js's own comment calls \"the single loudest complaint\" and player.js:2 calls \"quality priority #1.\" The only art.js change is the additive `drawWeapon` export.\n\n**Do not edit `js/rig.js`.** `RIG.poseFor` (145-149) does not consult `p.carrying` and does not need to. Because equipped weapons do not touch `carrying`, the V-key comparison view stays consistent with the sprite view. `drawWeapon` is skipped in rig mode.\n\n**Do not edit `Player._grabOrThrow` (185-201) or `_throw` (203-217).** The grab button keeps exactly one meaning. Do not add a `wepId` radius widening, a `recall` flag, or an equipped-weapon branch. On touch there is exactly one GRAB button (`index.html:54`) and no separate drop.\n\n**Do not edit `World._vsBody` (engine.js:148-192), and in particular the `nudge` closure (169-172).** Commit cca6a88 bought the measured guarantee that running the full length of the office is 0 damage, 0 objects destroyed, 0 boss anger. Every new force in this plan goes through the explicit `d.kbX/kbY × mulK` + `s.damageBody` path in `_swing` / `slamShock`. No weapon adds body-vs-body force. The equipped weapon is not in `world.bodies` at all, so it cannot shove anything by existing.\n\n**Do not clone, spread or derive a row of `ATTACK`.** `a.d` must be a *reference* to `ATTACK.heavy` or `ATTACK.light[step]`. `d === ATTACK.heavy` is load-bearing in three places — player.js:152 (the lunge), :173 (16 sparks vs 9, 420 spread vs 260), :175 (hit-sound power) — and a derived object silently strips all three while keeping heavy's timings, reading as \"the heavy got worse\" with no error.\n\n**Do not mutate `ATTACK` in place**, and do not `Object.assign(row.stats, mod)`. `statsFor` returns `Object.assign({}, row.stats, ...mods)` — a fresh object every attack. Mutating the module literal has no reset path.\n\n**Do not add a per-weapon chain length.** `ATTACK.light.length` stays the modulus at player.js:75 and `a.step` stays in range, so `d === undefined` inside `_stepAttack` — which throws, never clears `this.atk`, and kills movement, jump, dodge *and* grab for the rest of the shift with no bail-out — is structurally impossible.\n\n**Do not add `S.career` to the `startShift` reset block (main.js:222-234).** That block is a whitelist of 13 similar assignments; adding to it by pattern-match is the obvious mistake and it is the one that wipes the bank every shift.\n\n**Do not reuse the `.sw` class for shop cards.** `refreshSwatches` (main.js:180-183) queries `document.querySelectorAll('.sw')` unscoped against `S.look` and strips `.on` off anything matching, on every creator click and every RANDOMISE press (line 720).\n\n**Do not substitute the bank or the combined coin figure into the QUIET_RANKS test at main.js:303.** It reads `S.salary`, the per-shift wage, and nothing else.\n\n**Do not skip `hide('shop')` in `startShift`'s teardown list (main.js:245).**\n\n**Do not pass the equipped weapon to `S.damageBody` under any circumstance.** `type: 'weapon'` already makes it early-return at main.js:76, but the `W.wear > 0` gate in `_swing` means the call is never made. Both belts are intentional.\n\n---\n\n## 8. LAND ORDER (each step is independently testable and shippable)\n\n1. **Bug fixes only, no features** — `a.d`/`a.W` snapshot in `_startAttack`/`_stepAttack`; `wep.held = false` at player.js:179; the arc-tell fix at main.js:546-551. Provably no-ops or strict improvements. Commit alone today.\n2. **Economy** — `S.earn`, `S.salary`/`S.chaosCoins`, `chaos.js:35` depth-1-pays-zero, `events.js:33`, `WAGE`, re-thresholded `QUIET_RANKS`, `main.js:296`/`:303`. Play three shifts, one loud and one quiet, and confirm both land in 10–13k.\n3. **Persistence** — `js/save.js`, `S.career` in the literal, the debounce + `pagehide` flush, `#pBank`. Verify: earn, press R mid-shift, refresh the page, bank survives.\n4. **Shop screen** — `#shop`, `buildWeaponRows`, `hide('shop')` in the teardown. Buy `chair` with only `fists` implemented.\n5. **Equip plumbing** — `p.equipped`, `makeWeapon`, `equipShiftWeapon`, `statsFor`, `#hWeapon`, greybox `drawWeapon`. All six weapons swing as stat blocks with no specials yet.\n6. **Hand attach** — `HANDS`, the H-key calibration overlay, the `'hand'`/`'fixed'` switch.\n7. **Specials, one weapon per commit** — spin, burst, clang, slam+demolition, ride. Each ships with its counters and its two skills.\n8. **Art** — `SIZES` entries in `tools/cutout-world.py`, run `pass8-weapons.js`, collect, verify `props.json`.",
    "ranking": [
      {
        "name": "ONE SLOT — weapons as the carried object, specials as the heavy",
        "total": 18
      },
      {
        "name": "PAYROLL — Two Payslips, One Bank",
        "total": 16
      }
    ]
  },
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Map"
    },
    {
      "type": "workflow_phase",
      "index": 2,
      "title": "Design"
    },
    {
      "type": "workflow_phase",
      "index": 3,
      "title": "Judge"
    },
    {
      "type": "workflow_phase",
      "index": 4,
      "title": "Synthesize"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "map:combat",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "ab9dace8b414f2841",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788116069047,
      "queuedAt": 1788116069031,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Player combat / attack state machine and the carried-object…",
      "promptPreview": "Read C:/Users/it/Desktop/Worst Employee/js/player.js and C:/Users/it/Desktop/Worst Employee/js/config.js in full.\nExplain EXACTLY how attacks work: the attack state machine (startup/active/recover), how ATTACK.light steps and the combo counter work, how _swing builds its hitbox and applies damage/knockback, how the existing \"carrying\" weapon-swing works (a.wep, mul, carryPose), and how PLAYER tuni…",
      "lastProgressAt": 1788116393432,
      "tokens": 85471,
      "toolCalls": 21,
      "durationMs": 324384,
      "resultPreview": "{\"area\":\"Player combat / attack state machine and the carried-object \\\"weapon\\\" path (Worst Employee)\",\"summary\":\"An attack is a single mutable object `this.atk = { kind, step, phase, t, hit:Set, wep }` built in `Player._startAttack` (player.js:109-119) and driven by `Player._stepAttack` (player.js:121-138), which re-resolves its move data EVERY frame with `const d = a.kind === 'heavy' ? ATTACK.he…"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "map:state-persistence",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a13a2aa0eb1777726",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788116069048,
      "queuedAt": 1788116069031,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Worst Employee — shift lifecycle, coin economy, and look pe…",
      "promptPreview": "Read C:/Users/it/Desktop/Worst Employee/js/main.js and C:/Users/it/Desktop/Worst Employee/js/character.js in full.\nExplain EXACTLY: how the S state object is built and reset in startShift(), how coins are accumulated and where they are reset, how endShift() computes the report, and how the character look is persisted (saveLook/loadLook, localStorage key, versioning).\nI am adding a PERSISTENT coin …",
      "lastProgressAt": 1788116242449,
      "tokens": 80877,
      "toolCalls": 12,
      "durationMs": 168572,
      "resultPreview": "{\"area\":\"Worst Employee — shift lifecycle, coin economy, and look persistence (C:\\\\Users\\\\it\\\\Desktop\\\\Worst Employee\\\\js\\\\main.js + character.js, with chaos.js / events.js / player.js / office.js / config.js as the coin and weapon call sites)\",\"summary\":\"`S` in C:\\\\Users\\\\it\\\\Desktop\\\\Worst Employee\\\\js\\\\main.js:27-96 is a single module-level `const` object literal built ONCE at module evaluation…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "map:art-pipeline",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "ab0143b121dc7238e",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788116242455,
      "queuedAt": 1788116069031,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Sprite loading + draw pipeline (js/art.js, js/main.js rende…",
      "promptPreview": "Read C:/Users/it/Desktop/Worst Employee/js/art.js in full and C:/Users/it/Desktop/Worst Employee/tools/cutout-world.py.\nExplain EXACTLY how sprite loading works: the SPRITES object (player frames, outfit switching, recolouring), the CAST object, and the WORLD object (props.json, per-prop sizing, drawProp, the ss supersample field).\nI am adding weapon sprites that must be drawn HELD IN THE PLAYER'S…",
      "lastProgressAt": 1788116488694,
      "tokens": 99293,
      "toolCalls": 29,
      "durationMs": 246239,
      "resultPreview": "{\"area\":\"Sprite loading + draw pipeline (js/art.js, js/main.js render(), js/player.js carryPose, tools/cutout*.py) — as it exists today, and the seams for a hand-held, swing-rotated weapon sprite\",\"summary\":\"There are four independent sprite systems in js/art.js. `ART` (lines 13-52) is the original horizontal-strip loader — `ART.load({key:{src,frames,fps,anchorY}})` stores `{img,frames,fw:img.widt…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "map:ui-screens",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "ad30346ef38793dbb",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788116393436,
      "queuedAt": 1788116069032,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Worst Employee — screen structure/switching (index.html + j…",
      "promptPreview": "Read C:/Users/it/Desktop/Worst Employee/index.html and C:/Users/it/Desktop/Worst Employee/css/game.css in full, plus the screen-management code in C:/Users/it/Desktop/Worst Employee/js/main.js (show/hide, openCreator, buildOptionRows, startShift, endShift).\nExplain EXACTLY how screens are structured and switched, how the character-creator option rows are generated from data, and the CSS convention…",
      "lastProgressAt": 1788116666759,
      "tokens": 101962,
      "toolCalls": 20,
      "durationMs": 273323,
      "resultPreview": "{\"area\":\"Worst Employee — screen structure/switching (index.html + js/main.js), the data-driven character-creator option rows (js/character.js `OPTIONS` → main.js `buildOptionRows`), and the CSS conventions in css/game.css; plus every seam a SHOP screen and a HUD weapon-select would have to touch.\",\"summary\":\"There is no router and no framework: every screen is a plain `<div class=\\\"screen hidden\\…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "map:props-world",
      "phaseIndex": 1,
      "phaseTitle": "Map",
      "agentId": "a48512f4fad32c5d5",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788116488698,
      "queuedAt": 1788116069032,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Worst Employee — prop table, Body physics, damage/destructi…",
      "promptPreview": "Read C:/Users/it/Desktop/Worst Employee/js/office.js and C:/Users/it/Desktop/Worst Employee/js/engine.js in full.\nExplain EXACTLY: the PROPS table and makeProp, how Body mass/hp/value work, how damageBody and destruction work (in main.js S.damageBody), the grab/throw system, and how _vsBody resolves player-vs-prop pushing (note the recent change making destruction opt-in).\nI am adding weapons that…",
      "lastProgressAt": 1788116752602,
      "tokens": 88481,
      "toolCalls": 20,
      "durationMs": 263903,
      "resultPreview": "{\"area\":\"Worst Employee — prop table, Body physics, damage/destruction, and the grab/carry/swing system (js/office.js, js/engine.js, js/main.js S.damageBody, js/player.js)\",\"summary\":\"Every physical thing in the game is one `Body` (js/engine.js:9-40) living in the flat `World.bodies` array; `PROPS` (js/office.js:10-22) is a kind→`{w,h,mass,hp,value,label,color}` table read only by `makeProp` (js/o…"
    },
    {
      "type": "workflow_agent",
      "index": 6,
      "label": "design:minimal-diff",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "a688a2af8a2cf91a3",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788116752650,
      "queuedAt": 1788116752611,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "ONE SLOT — weapons as the carried object, specials as the h…",
      "promptPreview": "You are designing a WEAPONS + COIN-UNLOCK system for \"Worst Employee\", a 2D side-scrolling office-chaos brawler.\n\nHere is how the codebase ACTUALLY works today, mapped by readers who read every file:\n\n### Player combat / attack state machine and the carried-object \"weapon\" path (Worst Employee)\nAn attack is a single mutable object `this.atk = { kind, step, phase, t, hit:Set, wep }` built in `Playe…",
      "lastProgressAt": 1788117336636,
      "tokens": 123887,
      "toolCalls": 16,
      "durationMs": 583983,
      "resultPreview": "{\"name\":\"ONE SLOT — weapons as the carried object, specials as the heavy\",\"oneLiner\":\"A bought weapon is just a Body spawned into `this.carrying` at shift start, so `_startAttack`'s `const wep = this.carrying` (player.js:114), `_swing`'s reach/damage math, `carryPose`'s swing arc and the grab/throw path all keep working untouched; the only new machinery is a stats object snapshotted onto `this.atk…"
    },
    {
      "type": "workflow_agent",
      "index": 7,
      "label": "design:design-depth",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "a5348c139b2d81ffd",
      "model": "claude-opus-5[1m]",
      "state": "error",
      "startedAt": 1788116752652,
      "queuedAt": 1788116752612,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd \"C:/Users/it/Desktop/Worst Employee\" && echo \"=== engine…",
      "promptPreview": "You are designing a WEAPONS + COIN-UNLOCK system for \"Worst Employee\", a 2D side-scrolling office-chaos brawler.\n\nHere is how the codebase ACTUALLY works today, mapped by readers who read every file:\n\n### Player combat / attack state machine and the carried-object \"weapon\" path (Worst Employee)\nAn attack is a single mutable object `this.atk = { kind, step, phase, t, hit:Set, wep }` built in `Playe…",
      "lastProgressAt": 1788117153636,
      "error": "API Error: Connection lost mid-response. The response above may be incomplete.",
      "tokens": 105028,
      "toolCalls": 14,
      "durationMs": 399257
    },
    {
      "type": "workflow_agent",
      "index": 8,
      "label": "design:economy",
      "phaseIndex": 2,
      "phaseTitle": "Design",
      "agentId": "aa6b592c581477d56",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788117153638,
      "queuedAt": 1788116752612,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "PAYROLL — Two Payslips, One Bank",
      "promptPreview": "You are designing a WEAPONS + COIN-UNLOCK system for \"Worst Employee\", a 2D side-scrolling office-chaos brawler.\n\nHere is how the codebase ACTUALLY works today, mapped by readers who read every file:\n\n### Player combat / attack state machine and the carried-object \"weapon\" path (Worst Employee)\nAn attack is a single mutable object `this.atk = { kind, step, phase, t, hit:Set, wep }` built in `Playe…",
      "lastProgressAt": 1788117544557,
      "tokens": 106126,
      "toolCalls": 12,
      "durationMs": 390918,
      "resultPreview": "{\"name\":\"PAYROLL — Two Payslips, One Bank\",\"oneLiner\":\"Coins split into CHAOS COINS (wrecking) and SALARY (existing quietly), both funnel through one `S.earn()` choke point that banks to localStorage continuously — so a stealth shift and a demolition shift pay the same, and no exit path can lose your money.\",\"weaponModel\":\"A weapon is a row in a new `js/weapons.js` `WEAPONS` array, keyed by STRING…"
    },
    {
      "type": "workflow_agent",
      "index": 9,
      "label": "judge:ONE SLOT — weapons as th",
      "phaseIndex": 3,
      "phaseTitle": "Judge",
      "agentId": "a892fdad998e0ca0d",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788117544590,
      "queuedAt": 1788117544572,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Excellent codebase archaeology wrapped around a design that…",
      "promptPreview": "Adversarially review this proposed design for the \"Worst Employee\" game.\n\nCODEBASE REALITY:\n### Player combat / attack state machine and the carried-object \"weapon\" path (Worst Employee)\nAn attack is a single mutable object `this.atk = { kind, step, phase, t, hit:Set, wep }` built in `Player._startAttack` (player.js:109-119) and driven by `Player._stepAttack` (player.js:121-138), which re-resolves…",
      "lastProgressAt": 1788117926622,
      "tokens": 138449,
      "toolCalls": 16,
      "durationMs": 382031,
      "resultPreview": "{\"scores\":{\"fitsCodebase\":6,\"designDepth\":5,\"shipRisk\":4,\"progressionFeel\":3},\"fatalFlaws\":[\"ANIMATION COLLAPSE — the single worst regression, and the design brags about it. art.js:274 `if (p.carrying) return 'carry';` and art.js:261 `if (p.carrying) return 'swing';`. A bought weapon lives in `this.carrying` for the WHOLE SHIFT, so from the moment you own anything: run-1..run-6 never play (line 27…"
    },
    {
      "type": "workflow_agent",
      "index": 10,
      "label": "judge:PAYROLL — Two Payslips,",
      "phaseIndex": 3,
      "phaseTitle": "Judge",
      "agentId": "ab1b46514427b8e8a",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788117544590,
      "queuedAt": 1788117544573,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "A strong economy design bolted to a weapon design that does…",
      "promptPreview": "Adversarially review this proposed design for the \"Worst Employee\" game.\n\nCODEBASE REALITY:\n### Player combat / attack state machine and the carried-object \"weapon\" path (Worst Employee)\nAn attack is a single mutable object `this.atk = { kind, step, phase, t, hit:Set, wep }` built in `Player._startAttack` (player.js:109-119) and driven by `Player._stepAttack` (player.js:121-138), which re-resolves…",
      "lastProgressAt": 1788117788027,
      "tokens": 111273,
      "toolCalls": 10,
      "durationMs": 241283,
      "resultPreview": "{\"scores\":{\"fitsCodebase\":5,\"designDepth\":5,\"shipRisk\":3,\"progressionFeel\":3},\"fatalFlaws\":[\"THE CHAOS GUARD AS WRITTEN PAYS ZERO, FOREVER. The design supplies the stale check as \\\"REPLACES line 35\\\" of chaos.js. But `ignite()` mutates the body FIRST: line 28 `body.chaosUntil = this.s.time + CHAOS.chainWindow;` and line 29 `body.chainDepth = depth;`. The guard `body.chaosUntil > this.s.time && bod…"
    },
    {
      "type": "workflow_agent",
      "index": 11,
      "label": "synthesize-plan",
      "phaseIndex": 4,
      "phaseTitle": "Synthesize",
      "agentId": "ac1cd03df37426870",
      "model": "claude-opus-5[1m]",
      "state": "done",
      "startedAt": 1788117926636,
      "queuedAt": 1788117926634,
      "attempt": 1,
      "lastToolName": "Bash",
      "lastToolSummary": "cd \"C:/Users/it/Desktop/Worst Employee\" && sed -n '80,160p'…",
      "promptPreview": "Write the definitive implementation plan for the WEAPONS + COIN-UNLOCK system in \"Worst Employee\".\n\nCODEBASE REALITY (from readers who read every file):\n### Player combat / attack state machine and the carried-object \"weapon\" path (Worst Employee)\nAn attack is a single mutable object `this.atk = { kind, step, phase, t, hit:Set, wep }` built in `Player._startAttack` (player.js:109-119) and driven b…",
      "lastProgressAt": 1788118364219,
      "tokens": 148446,
      "toolCalls": 14,
      "durationMs": 437583,
      "resultPreview": "# WORST EMPLOYEE — WEAPONS + COIN-UNLOCK: DEFINITIVE IMPLEMENTATION PLAN\n\nVerified against the live files (`js/player.js` 236 lines, `js/main.js` 746, `js/config.js` 120, `js/chaos.js` 83, `js/art.js` 511, `js/office.js` 225, `index.html` 159, `css/game.css` 197). Every line reference below is real.\n\n---\n\n## 0. THE FIVE DECISIONS THAT MAKE THIS BUILDABLE\n\nThese are settled. Everything downstream f…"
    }
  ],
  "totalTokens": 1189293,
  "totalToolCalls": 184
}